<Project>
  <PropertyGroup>
    <!-- Redirect intermediate output (obj) to a short path to resolve MAX_PATH limits on Windows -->
    <BaseIntermediateOutputPath>$(USERPROFILE)\Build\obj\$(MSBuildProjectName)\</BaseIntermediateOutputPath>
  </PropertyGroup>
</Project>
